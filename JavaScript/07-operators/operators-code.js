//operador de asignacion
let myNumer = 0;

//operador de adición
myNumer += 1;

//operador de sustracion
myNumer -= 1;

//operador de multiplicacion
myNumer *= 2;

//operador de division
myNumer /= 2;

//operador de módulo
myNumer %= 2;
//operador del doble igual (===) que indica igualdad estricta || valor (!==) indica diferencia esctrica
//operador del doble igual (==) que indica igualdad || valor (!=) indica que no son iguales
function operadorIgualdad(a,b) {
    if (a==b) {console.log('son iguales')}
 }

 operadorIgualdad(1, '1');

console.log();

//mayor que >

if (5 > 3) console.log('Paso Mayor')